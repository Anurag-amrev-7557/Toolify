version: '3.8'
services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    image: toolify-backend:local
    ports:
      - "5001:5001"
    environment:
      - FLASK_ENV=production
    volumes:
      - ./backend:/app
